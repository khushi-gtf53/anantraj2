/** @type {import('next').NextConfig} */
const nextConfig = {
  // basePath: '/anantraj',
  // assetPrefix: '/anantraj/',
  eslint: {
    ignoreDuringBuilds: true,
  },
  // output: 'export', 
};

export default nextConfig;
